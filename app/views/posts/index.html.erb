<p id="notice"><%= notice %></p>

<h1>Current User</h1>
<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Birthday:</strong>
  <%= @user.birthday %>
</p>

<p>
  <strong>Address:</strong>
  <%= @user.address %>
</p>

<p>
  <strong>Postal code:</strong>
  <%= @user.postal_code %>
</p>

<p>
  <strong>Password:</strong>
  <%= @user.password %>
</p>

<h1>All posts</h1>

<table>
  <thead>
    <tr>
      <th>Msg</th>
      <th>user_id</th>
      <th>updated time</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @posts.each do |post| %>
      <tr>
        <td><%= post.msg %></td>
        <td><%= post.user_id %></td>
        <td><%= post.updated_at %></td>
        <td><%= link_to 'Show', post_path(post) %></td>
        <td><%= link_to 'Edit', edit_post_path(post) %></td>
        <td><%= link_to 'Destroy', post_path(post), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Log out', main_path %> |
<%= link_to 'back to your posts', userPost_path %> |
<%= link_to 'New Post', new_post_path %>
